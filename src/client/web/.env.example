# Environment variables needed to connect to the self-hosted LiveKit server.
#
# For local development with docker-compose, use these defaults:
LIVEKIT_API_KEY=devkey
LIVEKIT_API_SECRET=devsecret1234567890abcdefghijklmn
LIVEKIT_URL=ws://localhost:7880

# For production deployment, generate secure keys using:
# docker run --rm livekit/livekit-server generate-keys

# Internally used environment variables (optional)
NEXT_PUBLIC_APP_CONFIG_ENDPOINT=
SANDBOX_ID=
