"""Orchestrator server with LiveKit Agent and WebSocket fallback."""

from typing import Any


async def start_server(config: dict[str, Any]) -> None:
    """Start the orchestrator server with WebRTC and WebSocket transports.

    Args:
        config: Server configuration dictionary
    """
    pass
